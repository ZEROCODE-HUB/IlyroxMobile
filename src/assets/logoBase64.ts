/**
 * Stub para el logo en base64.
 *
 * Si se necesita el logo real embebido (por ejemplo en PDFs generados offline),
 * convertir src/assets/Logo.jpeg a base64 y reemplazar el valor de LogoBase64
 * (formato: "data:image/jpeg;base64,....").
 */
export const LogoBase64: string = "";
