import MapView, { Marker, PROVIDER_DEFAULT, PROVIDER_GOOGLE } from 'react-native-maps';
export { MapView as default, Marker, PROVIDER_DEFAULT, PROVIDER_GOOGLE };
export type { Region, MapPressEvent } from 'react-native-maps';
